

import 'package:flutter/material.dart';

class ListPatientItem extends StatelessWidget {
  const ListPatientItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
